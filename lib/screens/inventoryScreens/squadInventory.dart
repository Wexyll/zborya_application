import 'package:flutter/material.dart';

class squadInventory extends StatelessWidget {
  const squadInventory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("SquadInventory"),
    );
  }
}
