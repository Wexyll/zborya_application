import 'package:flutter/material.dart';


class logisticsPage extends StatelessWidget {
  const logisticsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Logistics Page"),
    );
  }
}
